{# templates/index.html.j2 #}

<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://unpkg.com/flowbite@1.5.4/dist/flowbite.min.css" />
  <script src="https://cdn.tailwindcss.com"></script>
  <title>sus : {{ file_name }}</title>
</head>

<body class="bg-zinc-900 text-orange-200">
  <div class="flex justify-center h-screen">
    <code id="tokens" class="text-stone-500 w-2/3 text-sm border border-stone-600 rounded-sm p-4">
  <div>
    {%- for token in tokens -%}
      {%- if token.original == '\n' -%}
        <br>
      {%- else -%}
        <span 
          data-tooltip-target="tooltip-{{ loop.index }}"
          class="hover:border-stone-700 border-transparent border cursor-pointer {{ token.text_color }}"
          >{{- token.original.replace(' ', '&nbsp;')  -}}</span><div 
          id="tooltip-{{ loop.index }}" 
          role="tooltip" 
          class="flex flex-row absolute invisible z-10 py-2 px-3 text-sm font-medium font-mono text-white bg-stone-700 rounded-lg shadow-sm opacity-0 transition-opacity duration-100 tooltip">
            {{- token.predicted.replace(' ', '&nbsp;') -}}
            <div class="tooltip-arrow" data-popper-arrow></div>
        </div>
      {%- endif -%}
    {%- endfor -%}
  </div>
  </code>
  </div>
  <script src="https://unpkg.com/flowbite@1.5.4/dist/flowbite.js"></script>
</body>

</html>